# ---------------------------
# AMPHIRIS OBJETS CLIQUABLES
# ---------------------------

on right click:
    # Objet qui envoie un message à Zarrrrrrrre
    if name of player's tool is "&6Exemple":
        cancel event
        make console execute command "/msg Zares_ Ceci est un exemple"
        stop